name: weather_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  geolocator: ^14.0.2
  permission_handler: ^12.0.1
  intl: ^0.20.2
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  lottie: ^3.1.2  # Thêm cho animation thời tiết từ video
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
  assets:
  - assets/images/clear_sky.png
  - assets/images/overcast.png
  - assets/images/rain.png
  - assets/images/fog.png
  - assets/images/thunderstorm.png
